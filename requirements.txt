# KMS Engine — 依赖声明
# pip install -r requirements.txt


# 核心依赖
tushare>=1.0
akshare>=1.0
pandas>=2.0
numpy>=1.24

# KMS 特有依赖
yt-dlp>=2023
faster-whisper>=1.0
imageio-ffmpeg>=0.4
websocket-client>=1.0
browser-cookie3>=0.19

# 开发依赖
pytest>=7.0
